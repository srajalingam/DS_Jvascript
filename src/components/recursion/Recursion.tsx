import React from 'react';

const Recursion: React.FC = () => {
    return (
        <div>
            <h2>Recursion Component</h2>
            <p>This is the Recursion component.</p>
        </div>
    );
};

export default Recursion;